package com.test;

import org.json.JSONException;
import org.json.JSONObject;

import com.util.StoreRegistrationDetails;

public class Test {
	public static void main(String[] args){
		/*NotificationBase notification = new AndroidNotification("Test", "Hello", "xxxx");
		SenderBase sender = new GCMSSender();
		try {
			sender.sendNotification(notification);
		} catch (CustomException e) {
			System.out.println(e.getMessage());
		}
		System.out.println("Hello");*/
		
		/*JSONObject test = new JSONObject();
		try{
			test.put("perm", true);
			test.put("username", "ABC");
			JSONObject platform = new JSONObject();
			platform.put("pName", "android");
			platform.put("pVersion", "4.4.2");
			test.put("pIdentifier", platform);
			test.put("device_token", "xxxxxxxx");
		}
		catch(JSONException e)
		{
			e.printStackTrace();
		}
		StoreRegistrationDetails.storeRegDetails(test);*/
	}
}
